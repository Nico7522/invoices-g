import { Password } from './password-directive';

describe('Password', () => {
  it('should create an instance', () => {
    const directive = new Password();
    expect(directive).toBeTruthy();
  });
});
